name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install
        run: pip install -e ".[dev]"
      - name: Lint
        run: ruff check sagaflow tests
      - name: Type-check
        run: mypy sagaflow
      - name: Test
        timeout-minutes: 5
        run: pytest -v --ignore=tests/generic --ignore=tests/scenarios --ignore=tests/skills -k "not TestWorkflowAbortSignal and not TestWorkflowStatusQuery"
